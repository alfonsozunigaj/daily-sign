$('.categoryDelete').bind('ajax:success', function () {
    let target = $(this).data('target');
    $('#' + target)[0].remove();
    let children = $(this).data('children');
    $('#' + children)[0].remove();
    $(this).remove();
});